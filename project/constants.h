#ifndef CONSTANTS
#define CONSTANTS

#include <vector>
#include <memory>

const int BOARD_WIDTH = 11;
const int BOARD_HEIGHT = 15;
enum blockType{NONE, I, J, L, O, S, Z, T};
enum blockPos{FACEUP, FACEDOWN, FACELEFT, FACERIGHT};

#endif
